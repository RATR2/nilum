// Skript addon registering Nilum effects, conditions, and expressions.
// TODO: add the Skript dependency once a target version is decided.

dependencies {
    implementation(project(":nilum-common"))
    implementation(project(":nilum-paper"))
}
