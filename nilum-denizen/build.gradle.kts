// Denizen extension with commands and tags.
// TODO: add the Denizen dependency once a target version is decided.

dependencies {
    implementation(project(":nilum-common"))
    implementation(project(":nilum-paper"))
}
